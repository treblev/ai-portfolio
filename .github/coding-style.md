# Coding Style

## Python

- **No underscore separators in numeric literals.** Write `8000`, not `8_000`. Write `120000`, not `120_000`. Plain integers are readable enough; the separator adds visual noise without benefit.
